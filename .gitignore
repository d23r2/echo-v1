backend/.env
backend/data/
backend/__pycache__/
backend/**/__pycache__/
backend/venv/
backend/.venv/

frontend/node_modules/
frontend/dist/
frontend/.env

# Capacitor Android — generated project's machine-specific/build output
frontend/android/local.properties
frontend/android/cacerts-with-avast
frontend/android/app/build/
frontend/android/build/
frontend/android/.gradle/
frontend/android/capacitor-cordova-android-plugins/build/

# Tauri Windows — build output
frontend/src-tauri/target/

# Local SearXNG instance config (docker-compose.searxng.yml) — generated on first run
searxng-config/

.DS_Store

# ECHO Layer 0 — defense-in-depth additions. Nothing above this line
# changed; these are additive safety nets for artifacts that shouldn't ever
# be tracked, even though most already aren't (see
# ECHO_LAYER_0_INFRASTRUCTURE_FOUNDATION.md's security section).
.env
*.env.local
*.pem
*.key
!frontend/src-tauri/**/*.key.example

# Build/installer artifacts — Android APK, Windows MSI/EXE installers
*.apk
*.aab
*.msi
frontend/src-tauri/target/release/bundle/

# Logs and caches
*.log
.pytest_cache/
.ruff_cache/
.mypy_cache/
frontend/.vite/
backend/data/backups/

# ECHO Layer 3A Part 2D — self-modification sandbox/deploy git worktrees.
# Created and torn down by self_modification_sandbox.py; never meant to be
# committed or to show up as noise in `git status`.
.self_mod_sandboxes/

# Review/export bundles — never meant to be committed
*.zip

# Editor/OS noise
.vscode/
.idea/
Thumbs.db
