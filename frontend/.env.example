VITE_API_BASE_URL=http://localhost:8000

# ECHO Layer 0 — build-time app identity (safe defaults, no secrets here).
# Never put a real secret in a VITE_ variable — Vite inlines it into the
# built JS bundle, which ships to the browser in plain text.
VITE_APP_NAME=ECHO
VITE_APP_ENV=development
